# FastAPI 0.100+ zieht Pydantic v2 / pydantic-core (Rust) — auf Termux/Android ohne
# passendes Rust-Target nicht baubar. 0.99.x nutzt Pydantic v1 (reines Python).
fastapi==0.99.1
pydantic>=1.10.14,<2
# Kein [standard]: vermeidet watchfiles/uvloop/httptools — auf Termux/Android sonst
# Rust/Maturin-Builds (aarch64-unknown-linux-android) und pip-Fehler.
uvicorn==0.32.1
jinja2==3.1.4
python-multipart==0.0.17
itsdangerous==2.2.0
# bcrypt braucht Rust zum Bauen — auf Termux oft nicht verfügbar. PBKDF2 (passlib) reicht hier.
passlib==1.7.4
openpyxl==3.1.5
# fpdf2 hängt Pillow als Pflicht an. PyFPDF 1.x (Paketname ``fpdf``) ohne Pillow — Termux-tauglich.
fpdf==1.7.2
pytest==8.3.4
# httpx 0.28: TestClient (Starlette 0.27) übergibt app= — inkompatibel; für Tests <0.28
httpx>=0.24.0,<0.28.0
